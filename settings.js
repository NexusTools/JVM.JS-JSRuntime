(function(JVM) {
    JVM.settings = {
        convertStack: true,
        dumpImpl: false,
        dumpStack: false,
        dumpSource: true,
        dumpOptimizations: false,
        verbose: true,

        // Optimizations
        allowOpt: true,
        optPasses: 1
    };
})(JVM);
