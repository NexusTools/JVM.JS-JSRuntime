(function(JVM) {
    JVM.settings = {
        convertStack: true,
        dumpImpl: false,
        dumpStack: false,
        dumpSource: false,
        dumpOptimizations: false,
        verbose: true,

        // Optimizations
        allowOpt: true,
        optPasses: 1
    };
})(JVM);
